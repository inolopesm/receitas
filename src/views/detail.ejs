<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Receitas | Detalhe</title>
</head>
<body>
    <h1>Receitas</h1>
    <h2><%= recipe.title %></h2>

    <ul>
        <li>
            <strong>Tipo de refeição:</strong> <%= recipe.mealType %>
        </li>
        <li>
            <strong>Quantidade de pessoas que o serve:</strong> <%= recipe.numberOfPeopleItServes %>
        </li>
        <li>
            <strong>Nível de dificuldade:</strong> <%= recipe.difficultyLevel %>
        </li>
        <li>
            <strong>Tipo de refeição:</strong> <%= recipe.mealType %>
        </li>
        <li>
            <strong>Tipo de refeição:</strong> <%= recipe.mealType %>
        </li>
    </ul>

    <h3>Lista de ingredientes</h3>
    <div style="white-space: pre-wrap;"><%= recipe.listOfIngredients %></div>

    <h3>Passo a passo para prepará-lo</h3>
    <div style="white-space: pre-wrap;"><%= recipe.preparationSteps %></div>
</body>
</html>
